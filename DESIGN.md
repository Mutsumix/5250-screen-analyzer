# Screen Friend - 技術設計書

## 1. アーキテクチャ概要

### 全体構成
```
┌─────────────────┐     ┌─────────────────┐
│  5250 Terminal  │     │  Screen Friend  │
│   (IBM i)       │────▶│    Desktop App  │
└─────────────────┘     └─────────────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │   AI Service    │
                        │ (Claude/GPT)    │
                        └─────────────────┘
```

### コンポーネント構成
- **Electronメインプロセス**: アプリケーション管理、システムAPI連携
- **レンダラープロセス**: UI表示、ユーザーインタラクション
- **画面キャプチャモジュール**: 定期的な画面取得
- **OCRモジュール**: 画面テキスト認識
- **AI連携モジュール**: プロンプト生成、レスポンス処理
- **状態管理**: 画面履歴、設定情報の管理

## 2. 技術スタック

### フロントエンド
- **Electron**: v28.x - デスクトップアプリフレームワーク
- **React**: v18.x - UIライブラリ
- **TypeScript**: v5.x - 型安全性
- **Tailwind CSS**: v3.x - スタイリング
- **Vite**: v5.x - ビルドツール

### バックエンド処理
- **Python**: v3.11+ - OCR処理、画像処理
  - **Tesseract OCR**: 文字認識
  - **Pillow**: 画像処理
  - **OpenCV**: 画面差分検出
- **Node.js**: Electron連携、API通信

### 外部サービス
- **OpenAI API**: GPT-4使用時
- **Anthropic API**: Claude使用時

## 3. ディレクトリ構造
```
screen-friend/
├── src/
│   ├── main/              # Electronメインプロセス
│   │   ├── index.ts
│   │   ├── ipc-handlers/  # IPC通信ハンドラ
│   │   └── services/      # バックエンドサービス
│   ├── renderer/          # レンダラープロセス
│   │   ├── App.tsx
│   │   ├── components/    # Reactコンポーネント
│   │   ├── hooks/         # カスタムフック
│   │   └── stores/        # 状態管理
│   ├── python/            # Python処理
│   │   ├── ocr.py         # OCR処理
│   │   ├── capture.py     # 画面キャプチャ
│   │   └── detector.py    # 変化検出
│   └── shared/            # 共通型定義
├── public/                # 静的ファイル
├── scripts/               # ビルドスクリプト
└── tests/                 # テストファイル
```

## 4. 画面設計

### メインウィンドウレイアウト
```
┌─────────────────────────────────────────────┐
│  Screen Friend - RPG Learning Assistant     │
├─────────────────────────────────────────────┤
│ ┌──────────────┐ ┌────────────────────────┐ │
│ │              │ │                        │ │
│ │   Terminal   │ │    AI Assistant        │ │
│ │   Preview    │ │                        │ │
│ │              │ │  > 次はF3キーを押して  │ │
│ │              │ │    メニューに戻ります  │ │
│ │              │ │                        │ │
│ └──────────────┘ └────────────────────────┘ │
│                                             │
│ [▶ Start] [⏸ Pause] [⚙ Settings]          │
└─────────────────────────────────────────────┘
```

### 設定画面
- キャプチャ領域設定（ドラッグ＆ドロップで指定）
- AIプロバイダー選択（Claude/GPT）
- APIキー設定
- OCR言語設定
- 更新頻度設定（1-10秒）

## 5. データフロー

### 基本的な処理フロー
1. **画面キャプチャ**
   - 指定領域を定期的にスクリーンショット
   - Electronのdesktop capturer APIを使用

2. **OCR処理**
   - Pythonプロセスで画像をテキスト変換
   - Tesseract OCRを使用（日本語対応）

3. **変化検出**
   - 前回のキャプチャと比較
   - 有意な変化がある場合のみ次のステップへ

4. **AI問い合わせ**
   - 画面内容と履歴からプロンプト生成
   - APIを通じてAIに問い合わせ

5. **結果表示**
   - レスポンスを解析してユーザーに表示
   - 履歴として保存

## 6. 通信設計

### IPC（プロセス間通信）
```typescript
// メインプロセス → レンダラー
- capture:result     // キャプチャ結果
- ocr:result        // OCR結果
- ai:response       // AI応答

// レンダラー → メインプロセス
- capture:start     // キャプチャ開始
- capture:stop      // キャプチャ停止
- settings:update   // 設定更新
```

### Python連携
- Node.js child_processでPythonスクリプトを実行
- JSONでデータをやり取り

## 7. セキュリティ考慮事項
- APIキーは暗号化して保存（electron-store使用）
- contextIsolation: true で安全性確保
- 外部スクリプトの実行を制限

## 8. パフォーマンス最適化
- 画面キャプチャは差分検出で効率化
- OCR処理は変化があった部分のみ
- AI APIコールは必要最小限に
- レスポンスのキャッシュ機能

## 9. エラーハンドリング
- ネットワークエラー時の再試行
- OCR失敗時のフォールバック
- API制限到達時の通知

## 10. 今後の拡張性
- 複数のターミナルウィンドウ対応
- カスタムプロンプトテンプレート
- 学習進捗の可視化
- エクスポート機能（学習履歴）